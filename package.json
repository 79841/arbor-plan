{
  "name": "arbor-plan",
  "private": true,
  "version": "1.0.0",
  "description": "Claude Code Plan 파일을 트리 구조로 시각화하는 도구",
  "author": "Dylan",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
